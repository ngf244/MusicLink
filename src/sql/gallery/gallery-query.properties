getMyPostListCount=SELECT COUNT(*) FROM ML_GALLERY WHERE USER_CODE=?
selectMyGalleryList=SELECT * FROM MY_GALLERY_LIST WHERE RNUM BETWEEN ? AND ? AND USER_CODE=?
selectRecentMyGalList=SELECT RECENT, GL_CODE, GL_TITLE, GL_CONTENT, GL_DATE, GL_READCNT, GL_STATUS, USER_CODE FROM (SELECT RANK() OVER(ORDER BY RNUM) AS RECENT, GL_CODE, GL_TITLE, GL_CONTENT, GL_DATE, GL_READCNT, GL_STATUS, USER_CODE FROM MY_GALLERY_LIST WHERE USER_CODE=?) WHERE RECENT < 6